# AGENTS.md

## 项目概览

公开税务案例库 - 单页 HTML 应用，收录 A 股/港股/美股/新三板（北交所）上市公司及典型非上市企业税务案例 169 条，支持筛选、搜索、详情弹窗查看。

**收录范围与口径**：时间为 2025 年 1 月至今；上市公司案例不限金额全部收录；非上市公司/个人案例仅收录补税/处罚总金额 ≥ 500 万元的，以及行业警示价值高的骗取出口退税、虚开发票、涉税中介违法等典型案件。

## 技术栈

- 原生 HTML/CSS/JavaScript（单文件）
- Node.js 静态文件服务器（server.js）
- 部署方式：`node server.js`（端口 5000）

## 项目结构

```
/workspace/projects/
├── index.html              # 核心文件 - 税务案例库（169条案例，全部数据内嵌）
├── analysis.html           # 数据看板（由 build-analysis.js 生成，勿手改）
├── operation-manual.html   # 独立操作台页面（与案例库独立，无导航链接）
├── server.js               # Node.js 静态文件服务器
├── build-analysis.js       # 看板生成器：node build-analysis.js 从 cases 生成 analysis.html
├── scripts/
│   ├── data-lint.js        # 数据质检+确定性自动修复（node scripts/data-lint.js [--fix]）
│   ├── daily-collect.js    # 每日案例采集：coze-coding-ai 搜索→查重→门槛初筛→产出候选报告（不自动写库）
│   ├── run-daily-tasks.sh  # 定时任务入口：collect(5点采集) / lint(7点质检+重建) / all
│   └── README-cron.md      # 定时调度配置说明（GitHub Actions / crontab）
├── reports/                # 采集候选报告 collect-YYYY-MM-DD.md/.json（待人工确认，勿入统计）
├── .coze                   # 部署配置（entrypoint=index.html）
└── AGENTS.md               # 本文件
```

## 自动化脚本（每日定时任务）

静态托管环境无常驻 cron，定时需挂在外部调度器（GitHub Actions / 云服务器 crontab，见 `scripts/README-cron.md`）。

- **每日采集（建议 5:00）** `bash scripts/run-daily-tasks.sh collect` → 调 `coze-coding-ai search` 多角度搜索近期案例，与现有 cases 按公司名/代码/全称查重，按门槛（上市公司全收；非上市≥500万或高警示价值）初筛，产出 `reports/collect-YYYY-MM-DD.md` 候选清单。**只出候选、绝不自动写库**——新案例的事实/金额拆分/分类需人工核实后按本文件规范录入 index.html。
- **每日质检（建议 7:00）** `bash scripts/run-daily-tasks.sh lint` → `data-lint.js --fix` 自动修复**所有有确定规则可循**的问题：region 简写→全称、A股裸代码补 .SH/.SZ/.BJ 后缀、enterprise 杂值归一（民营企业→民营、国有/国有企业→国企、个人/空/'-'→其他等）、typeClass 与 type 不匹配→按 id 精确修正样式类、date 非 YYYY-MM-DD（2026/9/1、2026年9月1日等）→自动标准化（均在 /tmp/taxcase-lint-backup 备份后修改）；**只报告不擅改**（缺信息/需业务判断）：必填字段缺失、type 非法值、裸代码无法判交易所、疑似重复案例、补税/处罚类未解析到金额、来源为 http。随后 `build-analysis.js` 重建看板。退出码 0=无 error，1=有需人工处理项。
- **手动**：`node scripts/data-lint.js`（只巡检不改文件）；巡检退出码 0=无 error，1=有需人工处理项。


## 数据模型

**卡片展示规则（统一，勿在数据里重复）：**
- 公司简称：直接用 `company` 字段，填干净的简称，**不要在简称里带股票代码/编号**（代码下方单独展示）；需要区分同公司多次事件时用括号副标题（如"丰林集团年内第二次补缴"、"云南智辉（安车检测持股平台）"）。渲染时统一经 `displayCompany()` 清洗。
- 板块/代码：`code` 只填代码本身（A股6位数字如 `600396`、港股如 `03678.HK`、美股如 `APH.US`、非上市填 `非上市`），渲染时统一经 `formatCode()` 自动加板块前缀，输出形如"主板 600396""创业板 300755""科创板 688212""北交所 871975""港股 03678""美股 APH"，A+H 多代码用 ` / ` 连接。板块判定：688/689=科创板，300/301=创业板，43/83/87/88/92/93 开头6位=北交所，其余6位=主板。

每条案例数据对象包含：
- `id` - 唯一编号
- `company` - 公司简称
- `company_full` - 公司全称
- `code` - 股票代码
- `region` - 所属地区（省/市）
- `date` - 披露日期
- `type` - 案例类型（固定5类，顺序锁定：自查补缴 / 稽查处罚 / 行政处罚 / 失信虚开 / 行业参考）
- `typeClass` - 类型样式类
- `industry` - 所属行业
- `taxTags` - 涉及税种数组
- `industryTag` - 行业标签
- `natureTag` - 性质标签
- `amountDisplay` - 金额显示
- `amountDesc` - 金额描述
- `totalAmount` - 总金额
- `taxBreakdown` - 税务明细数组；**每项统一用 `{name, amount}` 字段**（name=项目名、amount=金额），不要用 `item`
- `reportImpact` - 报表影响数组；每项可写 **`{item, impact}` 对象**（如 {item:"当期损益", impact:"计入2025年度"}）或**直接写一句话字符串**（如"补缴税款计入2026年度当期损益"），弹窗渲染两种都兼容
- `outcome` - 处理结果与进度对象（可选），结构 `{status: "done"|"progress", notes: [字符串...]}`；`done`=已缴清/已处理完毕（绿色"已处理完毕"），`progress`=诉讼/审理/追缴中等（橙色"处理中/待跟进"）；notes 为该案例最新处理进展要点（如已缴清金额、是否涉处罚、诉讼阶段、以终审为准等）。新案例有明确处理状态时填写；后续进展更新就更新此字段
- `keyPoints` - 关键要点数组（<li> 渲染）
- `referenceValue` - 参考价值文本
- `link` - 来源链接
- `linkText` - 链接文字

**详情弹窗模块（modalBody，自上而下）**：基本信息 → 关键要点 → 税务明细 → 报表影响 → **处理结果与进度（outcome，有才显示）** → 参考价值 → **相关案例（自动计算，最多3条）** → 来源链接。

- **相关案例**：`getRelated(id, limit=3)` 自动推荐，无需手动填写。规则：同一公司主体（去掉括号副标题后名称包含）+100分"同一主体"；同行业 +15；共享税种 +8；同类型 +6；同地区 +3；按分排序取前3，至少1条才显示。点击相关案例卡片直接 `openModal(id)` 跳转。
- 新增/补录案例后须确保 **`id` 按数组顺序 1..N 连续**（质检脚本可核对；`data-lint.js` 不自动改 id，重排由专项脚本处理）。

## 关键功能模块

1. **筛选系统**：按类型/行业/地区/企业性质/税种五级下拉筛选（currentType/currentIndustry/currentRegion/currentEnterprise/currentTax）+ 开始日期/结束日期区间筛选（currentStartDate/currentEndDate，按 `date` 字段 YYYY-MM-DD 字符串比较）；"重置筛选"会一并清空日期
2. **搜索功能**：搜索公司名称、案例描述、关键词
3. **卡片视图**：展示案例卡片，含公司名、代码、地区、类型、金额
4. **表格视图**：带排序功能的数据表格
5. **详情弹窗**：基本信息、关键要点、税务明细、参考价值
6. **仪表盘**：按类型统计的案例数量面板

## 重要函数

- `renderDashboard(data)` - 渲染仪表盘统计
- `renderCards(data)` - 渲染卡片视图
- `renderTable(data)` - 渲染表格视图
- `applyFilters()` - 应用筛选/搜索条件
- `openModal(id)` - 打开案例详情弹窗
- `filterByDash(type)` - 按仪表盘类型筛选
- `initDashboardFilters()` - 初始化筛选下拉菜单
- `renderCharts()` - 渲染仪表盘4个分布图表（省份/企业性质/行业/税种），从 `cases` 动态统计，页面加载时自动调用

## 数据看板（build-analysis.js → analysis.html）

- 生成方式：`node build-analysis.js` 从 index.html 提取 `cases`，注入模板生成 analysis.html；**analysis.html 勿手改**。
- **主题色：绿色系**（emerald/teal，主色 `--primary:#059669`），与案例库蓝色明确区分；红色仅用于风险/罚款/警示等语义。
- 结构：①整体税务环境概览（KPI、类型/月度/行为年度/追溯周期/企业性质/税种/金额区间/金额构成/行业/地区/成本对比+环境洞察）；②风险定位·高频场景与重点行业地区（标题下有一段绿色 `section-lead` 总结；含高频风险场景、行业×类型堆叠、地区税费金额 TOP5、处置类型成本对比 4 图）；③白酒专项（banner、KPI、四主线、紧凑图表——类型甜甜圈与金额构成并排+税种横图，均 `maintainAspectRatio:false` 固定矮高与四主线等高、明细表、3列×2行共6张结论卡片、3列合规自检清单）。
- 所有图表均从 `cases` 动态统计，新增/修改案例后重建看板即可同步，无需改图表代码。
- 板块归属：客户端 `boardOf(code)`（688/689科创板、300/301创业板、43/83/87/88/92/93或4/8开头北交所、HK港股、纯字母/US美股、其余主板），与案例库规则一致。
- 高频风险场景：`SCENARIOS` 关键词正则归类（一案可命中多场景），如新增风险维度在此数组补充即可。
- 类型配色 `TYPE_COLORS` 固定：自查补缴绿/稽查处罚橙/行政处罚红/失信虚开紫/行业参考青(teal)。

### KPI 顶部卡片锁定规则（#kpiGrid，勿随意增删/改口径）

「一、整体税务环境概览」顶部固定 **4 张** KPI 卡片（用户于 2026 年明确指定：案例总数、可统计税费本金、滞纳金、罚款/罚没），**顺序、文案、口径、颜色一经锁定，不得在新增/修改案例时改动**；卡片数值由 `cases` 实时统计（数值随数据变动属正常），但**卡片集合与定义只有用户明确要求才能调整**。渲染代码固定在 build-analysis.js 的 `document.getElementById('kpiGrid').innerHTML=[...]`：

| # | 数字（.num） | 标签（.label） | 取值口径 | 配色 class |
|---|---|---|---|---|
| 1 | `total` = `cases.length` | 收录案例总数 | 全部案例条数 | `''`（绿） |
| 2 | `principalDisplay` = `yi(sumTaxPrincipal)` +（有不可统计案例时）`+` | 可统计税费本金（N 例可统计，余 M 例未披露/非税费） | `sumTaxPrincipal` = `sumTotal − sumLatefee − sumFine`（= 严格本金 `sumPrincipal` + 未拆分税费合计 `sumMixed`）；即所有未单列滞纳金/罚款的税费合计全部归入本金；`+` 表示下限约数 | `teal`（青绿） |
| 3 | `yi(sumLatefee)` | 滞纳金 · 占本金 X%（年化约18.25%） | 全部案例 `latefee`（滞纳金 + 加收利息）合计；`latefeeNote` 附其占本金百分比 | `warning`（橙） |
| 4 | `yi(sumFine)` | 罚款 / 罚没 · 占本金 X%（换行小字：含行政罚款 · 刑事罚金 · 没收违法所得） | 全部案例 `fine`（行政罚款 + 刑事罚金 + 没收违法所得 + 罚没收入）合计；`fineNote` 附其占本金百分比 | `danger`（红） |

- **本金口径说明**：三段相加 `本金 + 滞纳金 + 罚款 = 可统计税费总额`（`sumTotal`）。此前版本有第 5 张"自查补缴占比"卡和一张"税费总额"卡，已按用户要求删除/改为本金卡；`selfCheck` 仍保留用于环境洞察文本，不再作为 KPI 卡片。
- **不可统计案例口径**：不纳入人民币税费合计的包括（a）外币计价且未给人民币换算括号（如安贤园中国 00922.HK 已在 2026-09 折人民币入本金，`taxBreakdown` 金额改为人民币、港元原值留在 note）；（b）明细全为业务金额（关联交易/营收/分红/采购/资金占用等，行业参考/行政处罚类多属此）；（c）仅披露虚开发票**价税合计/份数**、拆分收入等发票面值而未披露实际税款的案件（如垚来会计拆分收入4706万、天津空壳虚开2510万、广西庆美价税合计1.03亿）——这些是业务/票面额而非应缴税款，不计入税费。骗税案件"骗取出口退税款/挽回/流失税款"属**税款**，须计入本金（见 NONTAX_RE/PRINCIPAL_RE 中 `骗(取)?出口退税|出口退税款|挽回税款`）。
- 「税费金额构成」环形图（`chartComposition`）、「处置类型成本对比」（`chartTypeCost`）、白酒「税费金额构成」（`chartBaijiuComp`）三处图表的"本金"均采用**同一口径**（`total − latefee − fine`），保证图与 KPI 卡一致、三段可加总回总额，勿改回严格 `principal` 口径。
- 金额单位：`yi()` 自动换算，≥1亿显示"x.x亿"，否则显示"x万"。
- 本金/滞纳/罚款的归类规则见下方「数据看板金额口径」（`parseCaseMoney`），**归类逻辑与 KPI 卡片同样锁定**，改动需用户确认。
- 白酒专项另有独立的 5 张白酒 KPI（`#baijiuKpi`：白酒相关案例数/上市公司数/自查补缴/问询参考/稽查虚开），与全局 KPI 互不影响，口径另定。

## 数据看板金额口径（build-analysis.js）

看板（analysis.html）的税费金额由 `build-analysis.js` 中的 `parseCaseMoney(c)` 从案例数据动态解析，**不是简单读取 totalAmount**，而是按以下口径从 `taxBreakdown` 明细分类提取：

- **本金（principal）**：明细名称命中税种/本金关键词（补缴、补税、税款、企业所得税、增值税、消费税、资源税、出口退税追回、追加税项等）
- **滞纳金（latefee）**：明细名称含「滞纳金/加收利息」
- **罚款（fine）**：明细名称含「罚款/罚没/罚金/没收违法所得/行政处罚」
- **混合项（mixed）**：名称含税费字样但无法细分（如"补缴税款及滞纳金"），整项计入总额
- **自动排除的非税费业务金额**：资金占用、关联交易/采购/应收、分红/回购、营业收入/调减/虚增、拍卖/成交价、以物抵债、信用证、担保费、工程交易、虚开发票价税合计、拆分收入等
- **企业收到的退税（返还收入，非补税，`关税退税|收到退税款` 自动排除）**：如美国海关关税退税、税务机关退还的税款，不计入本金；但**追回/骗取/缴回的退税属补税须计入**（如"出口退税追回""骗取出口退税款""退回/缴回多退税款"，见 PRINCIPAL_RE）
- **外币（美元/港元等）**：若整条案例以外币计价且未给人民币换算括号，则不计入人民币合计（标记为未解析）

**新增/修改案例时为保证金额可被正确统计，需注意：**
- `taxBreakdown` 每项用 `name`（或 `item`）+ `amount` 描述，金额写中文单位（如 "1,921.19万元"、"约1.5亿元"）；分项不要写"未单独披露/—"占位（写了会自动回退到 totalAmount）
- 避免把业务金额（关联交易额、占用款、营收等）与税费金额混在同一个明细项
- 合计类明细名称以"合计/总计/累计/价税合计"开头，会自动跳过（由分项体现，避免重复累加）
- 纯数字 totalAmount 按万元处理；带千分位逗号也能正确解析

## 核心变量

- `cases` - 全部案例数据数组
- `currentType` / `currentIndustry` / `currentRegion` - 当前筛选状态
- `typeConfig` - 类型配置（含图标和样式类）

## 仪表盘自动同步规则

仪表盘数量面板与4个分布图表（省份分布、企业性质分布、行业分布、涉及税种）**均从 `cases` 数组动态计算**。**每新增/删除/修改一个案例，仪表盘会在页面加载时自动同步更新，无需手动改数字或改图表代码。**

但要保证统计准确，新增案例时字段命名必须遵循以下规范：

- `region`（地区）：使用完整行政区划名，如 `北京市`、`上海市`、`广东省`、`浙江省`、`江苏省`、`四川省`、`山东省`、`安徽省`、`辽宁省`、`黑龙江省`、`新疆维吾尔自治区`。禁止简写为 `北京`、`广东` 等，否则同一省份会被拆成两条统计
- `enterprise`（企业性质）：取值仅限 `民营`、`国企`、`央企`、`外企`；无法判断时填 `其他`，禁止使用 `-`、`个人` 等杂项
- `industry`（行业）：使用统一行业名称，如 `医药`、`白酒`、`新材料`、`能源`、`机械制造` 等
- `taxTags`（涉及税种）：使用统一税种名，如 `企业所得税`、`增值税`、`消费税`、`个人所得税`、`关税` 等
- `type` / `typeClass`：**固定5类、顺序锁定，严禁新增/更名/调整顺序**。`typeConfig`、筛选下拉、看板类型甜甜圈均以该顺序为准：
  1. **自查补缴**（type-selfcheck）：企业自查/主动申报、补缴税款+滞纳金、无行政处罚、不定性偷税（含税务局约谈后主动补、稽查后主动补正免罚；混改递延纳税被否补税、资源税/土地增值税等被动清算但未定性偷税的，如无罚款也归入此类；转让定价/特别纳税调整若为主动调整只加收利息，也归此类）
  2. **稽查处罚**（type-inspection）：**税务机关**稽查后追缴、定性偷税/欠税、有罚款/罚金/没收，或被动追缴税款（无主动自查）、收到《催告书》/税务处理处罚决定书；跨境转让定价被动补税（如安费诺）归此类
  3. **行政处罚**（type-violation）：证监/财政/交易所等**非税务机关**的行政处罚（信披违规、财务造假、资金占用、审计未勤勉尽责等），多无税款
  4. **失信虚开**（type-fraud）：核心违法行为是虚开发票（虚开/取得虚开/空壳开票、虚开收购发票），含涉票犯罪
  5. **行业参考**（type-reference）：无补税无处罚的合规做法/问询回复/关联交易定价参考/税收政策影响、税务局集中曝光或宣传案例、涉税中介/一案双查等警示通报
  - 判定优先级：涉票违法核心 → 失信虚开；非税务机关处罚 → 行政处罚；有罚款/定性偷税/被动追缴 → 稽查处罚；主动补税免罚 → 自查补缴；无补税无处罚的参考信息 → 行业参考

## 注意事项

- 所有数据硬编码在 index.html 的 `<script>` 中
- 更新数据时需保持 JS 对象语法正确，避免中文引号问题
- 新增案例需同时添加 `region` 字段
- 筛选、搜索、地区选项均从案例数据中动态提取（自动去重排序）
- 案例库与操作台互不关联，无导航链接